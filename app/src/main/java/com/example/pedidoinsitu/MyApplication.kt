package com.example.pedidoinsitu

import android.app.Application

class MyApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Inicialización global si necesitas (ej: librerías, logs, etc.)
    }
}
